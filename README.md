```python?code_reference&code_event_index=6
markdown_content = """# Database Systems Project 📊

Welcome to my Database course project repository. This repository contains all the deliverables, design documents, and code (SQL and others) developed during the course as a final project.

## 📝 Project Description
This project involves the design, creation, and management of a Relational Database simulating a complete information system. The system was designed to manage complex data, including various entities, relationships, business constraints, and support for complex queries for data retrieval.

## 🛠️ Technologies & Tools
* **Database Management System (DBMS):** SQL Server / MySQL / PostgreSQL *(Edit to leave only the relevant one)*
* **Integration/Backend Language (if applicable):** Python / Node.js / Java *(Edit as needed)*
* **Data Modeling:** Creation of Entity-Relationship Diagrams (ERD) and translation to a normalized relational schema.

## 📂 Repository Structure
* `docs/` - Project specifications, complete ERDs, normalized relational schema, and the Data Dictionary.
* `sql_scripts/` - Collection of SQL scripts:
  * `DDL.sql` - Commands for creating tables, defining Primary/Foreign Keys (PK/FK), and Constraints.
  * `DML.sql` - Commands for inserting mock data to test the system.
  * `Queries.sql` - Complex queries written for the project (Joins, Group By, Sub-queries, etc.).
* `src/` - Application code or interface connecting to the database *(if applicable)*.

## 📊 Data Model & Schema
In the initial phase of the project, a requirements specification was written based on the system's needs. From these requirements, an Entity-Relationship Diagram (ERD) was extracted, including 1:N and M:N relationships. The model then underwent normalization up to at least the Third Normal Form (3NF) to prevent data redundancy and ensure Data Integrity.

## 🚀 Installation & Setup
To run this project in your local environment, follow these steps:

1. Clone the repository to your local machine:
